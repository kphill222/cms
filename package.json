{
  "name": "cms-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cd server && npm run build",
    "preview": "vite preview",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["cms", "dashboard", "admin"],
  "author": "",
  "license": "ISC",
  "description": "A professional CMS dashboard with modern UI and responsive design.",
  "devDependencies": {
    "@vitejs/plugin-basic-ssl": "^2.0.0",
    "vite": "^5.0.0"
  }
}
